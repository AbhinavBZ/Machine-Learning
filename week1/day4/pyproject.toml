[project]
name = "day4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AbhinavBZ", email = "abhinav.chiron@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "groq>=1.7.0",
    "pydentic>=0.0.1.dev3",
    "python-dotenv>=1.2.3",
]

[project.scripts]
day4 = "day4:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
